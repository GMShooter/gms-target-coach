# Security overrides for vulnerable dependencies
# These overrides mitigate known vulnerabilities while maintaining compatibility
overrides.esbuild=^0.24.2
overrides.nth-check=^2.0.1
overrides.postcss=^8.4.31
overrides.webpack-dev-server=^5.2.0

# Audit configuration
audit-level=moderate
audit-force=false